apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gala-chart.fullname" . }}-test-connection"
  labels:
    {{- include "gala-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gala-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
